extends ../layout/index

block contenido 
    div.py-10
        h1.text-4xl.my-10.font-extrabold.text-center Bienes 
            span.font-normal Raices 
        h2.text-center.text-2xl.font-extrabold= pagina
        if errores 
            div(class="max-w-md mt-10 max-auto")
                each error in errores 
                    p(class=" bg-rose-800 uppercase text-xs text-center p-2 mb-1 font-bold")=error.msg
        div.mt-10.max-auto.max-w-md
            div.bg-white.py-8.px-4.shadow
                form.space-y-5(method="POST" novalidate)
             
                    input(type="hidden" name="_csrf" value=csrfToken)
                    div
                        label.block.text-sm.uppercase.text-gray-500.mb-2.font-bold(for="password") Tu password
                        input#password.w-full.px-3.py2.border.border-gray-300.rounded-md.placeholder-gray-400(placeholder="Tu nuevo password" type="password" name="password")
                    div.flex.items-center.justify-between
                        a.text-gray-500.text-xs(href="/auth/login") Ya tienes cuente?. Inicia sesión 
                        a.text-gray-500.text-xs(href="/auth/registroUsuario") ¿No tienes cuenta? Crea una
                    input(type="submit" value="Guardar password" class="w-full bg-indigo-600 hover:bg-indigo-700 rounded-md py-2 cursor-pointer text-white font-bold")